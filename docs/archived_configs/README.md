# Archived Configs

These configs document internal comparison, ablation, or legacy experiments.
They are kept for paper-traceability only and are not expected to run in a
fresh public clone without additional private data or unreleased checkpoints.

Use `configs/separate/` and `configs/sample/` for the public runnable examples.
